/// `vibe_check` is a Dart analyzer plugin, not an importable library.
///
/// Enable it under the top-level `plugins:` section of your
/// `analysis_options.yaml` and opt into rules via `diagnostics:`. There is no
/// public Dart API to import — the analyzer loads the plugin via
/// `lib/main.dart` automatically.
library;
